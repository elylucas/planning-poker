
import {Map, List, fromJS} from 'immutable';

export function createRoom(state = Map(), room) {
  // return state.push(fromJS(
  //   {
  //     roomId: roomId,
  //     users: [user]
  //   }));
  console.log(room);
  return state;
}
